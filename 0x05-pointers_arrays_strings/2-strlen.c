#include "main.h"
#include "string.h"

/**
 * _strlen - function that returns the length of a string.
 * @s: strin6g pointer to be passed to this function.
 * Return: returns the length of the string
 */

int _strlen(char *s)
{
	return (_strlen(s));
}
